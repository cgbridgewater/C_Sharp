<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Contacts!</title>
    </head>


<!-- html/head tags removed for brevity -->
<body>
    <h1>Testing C# insert methods</h1>
    <!-- Notice how "AT" starts our C# section
    and we have wrapped all our logic in a code block -->
    <h3>Example 1: inserting a loop</h3>
    @{    
        List<string> StringList = new List<string>() {"one", "two", "three", "four"};
        foreach(string word in StringList)    
        {        
            // We can render HTML from within C# code!
            // We just have to remember to bring @ back if we want to render variables
            <p>@word</p>    
        }
    }
    <br>

    <h3>Example 2: inserting values</h3>
    @{
        string word2 = "Hello";
        int number = 7;
    }
    <p>The word is: @word2</p>
    <p>The number is: @number</p>

    <br>
    <h3>Example 3: inserting a counter</h3>
    @for(int i = 0; i < 5; i++)
    {
        <p>@i</p>
    }

    <br>
    <h3>Example 4: inserting a greeting loop</h3>
    <p>This is some basic HTML</p>
    @{
        string greeting = "Hi there";
        int count = 5;
        for(int i = 1; i < count; i++)
        {
            <p>@greeting #@i</p>
        }
    }
    <p>Here's some more HTML for you!</p>
    <p>That loop ran @count times</p>


    <p>Example 5: running the loop from example 1 from a list placed elsewhere on the page (loop only) </p>
    @{
        foreach(string word in StringList)    
        {        
            <p>@word</p>    
        }
    }

</body>